\documentclass[25pt, a0paper, portrait]{tikzposter}
\usetheme{Rays}
\usecolorstyle{Default}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[english]{babel}
\usepackage{amsmath,amssymb}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows, calc, positioning}

\title{Makeitalive: Landscape picture animation}
\author{Mathurin Petit, Arthur Fournier}
\institute{CSC\_52002 - Generative AI Project}

\begin{document}
\maketitle

\begin{columns}
    \column{0.5}
    \block{1. Project Overview}{
        The \textbf{Makeitalive} project explores the transformation of a static landscape photograph into a realistic animation. 
        We preserve the original texture while injecting coherent movement.
        
        \vspace{1em}
        We came with 2 different ideas to solve this challenge :
        \begin{itemize}
            \item\textbf{Motion-flow} : we create a flow field to animate the image. This flow will move the pixels horizontally and vertically to generate a movement.
            \item\textbf{SVD} : we fine-tune a video generation model to animate the image on a specific dataset.
        \end{itemize}
    }
    
    \block{2. The Motion Flow Concept}{
        The approach is based on estimating an \textit{Optical Flow} field:
        \begin{itemize}
            \item Prediction of $(dx, dy)$ vectors per pixel.
            \item \textbf{Backward Mapping} for rendering:
            \[ I_{anim}(x, y, t) = I_{orig}(x - \alpha_t dx, y - \alpha_t dy) \]
        \end{itemize}
        \begin{center}
            \includegraphics[width=0.85\linewidth]{motion_flow_grid.png}
        \end{center}

        \block{Architecture UNet — MotionFlowUNet}{
    \begin{center}
    \resizebox{0.9\linewidth}{!}{%
    \begin{tikzpicture}[
    every node/.style={font=\small},
    enc/.style={draw=teal!70!black, fill=teal!15, rounded corners=4pt,
                minimum width=3.2cm, minimum height=0.9cm, align=center},
    bot/.style={draw=orange!80!black, fill=orange!20, rounded corners=4pt,
                minimum width=3.6cm, minimum height=1cm, align=center},
    dec/.style={draw=violet!70!black, fill=violet!15, rounded corners=4pt,
                minimum width=3.2cm, minimum height=0.9cm, align=center},
    out/.style={draw=gray!60,        fill=gray!10,   rounded corners=4pt,
                minimum width=3.2cm, minimum height=0.9cm, align=center},
    skip/.style={->, dashed, thick, orange!80!black,
                shorten >=2pt, shorten <=2pt},
    flow/.style={->, thick, gray!60!black,
                shorten >=2pt, shorten <=2pt},
    ]

    %% ── Encodeur (colonne gauche) ──────────────────────────────────────────
    \node[enc] (inc)   at (0, 0)    {\textbf{inc}\\\footnotesize 3→32 · $512^2$};
    \node[enc] (down1) at (0,-1.8)  {\textbf{down1}\\\footnotesize 32→64 · $256^2$};
    \node[enc] (down2) at (0,-3.6)  {\textbf{down2}\\\footnotesize 64→128 · $128^2$};
    \node[enc] (down3) at (0,-5.4)  {\textbf{down3}\\\footnotesize 128→256 · $64^2$};

    %% ── Bottleneck (centre-bas) ────────────────────────────────────────────
    \node[bot] (bot)   at (4,-7.2)  {\textbf{down4 — Bottleneck}\\\footnotesize 256→512 · $32^2$};

    %% ── Décodeur (colonne droite) ──────────────────────────────────────────
    \node[dec] (up1)   at (8,-5.4)  {\textbf{up1}\\\footnotesize 768→256 · $64^2$};
    \node[dec] (up2)   at (8,-3.6)  {\textbf{up2}\\\footnotesize 384→128 · $128^2$};
    \node[dec] (up3)   at (8,-1.8)  {\textbf{up3}\\\footnotesize 192→64 · $256^2$};
    \node[dec] (up4)   at (8, 0)    {\textbf{up4}\\\footnotesize 96→32 · $512^2$};

    %% ── Tête de sortie ─────────────────────────────────────────────────────
    \node[out] (outc)  at (8, 1.8)  {\textbf{outc — Conv $1{\times}1$}\\\footnotesize 32→2 · $512^2$};

    %% ── Flèches encodeur (vers le bas) ────────────────────────────────────
    \draw[flow] (inc)   -- (down1);
    \draw[flow] (down1) -- (down2);
    \draw[flow] (down2) -- (down3);
    \draw[flow] (down3) -- ++(0,-0.6) -| (bot);

    %% ── Flèche bottleneck → décodeur ──────────────────────────────────────
    \draw[flow] (bot) -- ++(0,0.6) -| (up1);

    %% ── Flèches décodeur (vers le haut) ───────────────────────────────────
    \draw[flow] (up1) -- (up2);
    \draw[flow] (up2) -- (up3);
    \draw[flow] (up3) -- (up4);
    \draw[flow] (up4) -- (outc);

    %% ── Skip connections (tiretés orange) ─────────────────────────────────
    \draw[skip] (down3.east) -- node[above,font=\tiny] {cat} (up1.west);
    \draw[skip] (down2.east) -- node[above,font=\tiny] {cat} (up2.west);
    \draw[skip] (down1.east) -- node[above,font=\tiny] {cat} (up3.west);
    \draw[skip]  (inc.east)  -- node[above,font=\tiny] {cat} (up4.west);

    %% ── Labels entrée / sortie ─────────────────────────────────────────────
    \node[above, gray, font=\footnotesize] at (inc.north)  {Entrée RGB ($512{\times}512$)};
    \node[above, gray, font=\footnotesize] at (outc.north) {Flow $(dx,\,dy)$};

    %% ── Légende ─────────────────────────────────────────────────────────────
    \matrix [draw=gray!40, rounded corners, below right, font=\footnotesize,
            row sep=4pt, column sep=6pt, inner sep=6pt]
    at ($(bot.south west)+(0,-0.8)$) {
    \node[enc, minimum width=1.6cm, minimum height=0.5cm]
            {\footnotesize Encodeur}; &
    \node[bot, minimum width=1.6cm, minimum height=0.5cm]
            {\footnotesize Bottleneck}; &
    \node[dec, minimum width=1.6cm, minimum height=0.5cm]
            {\footnotesize Décodeur}; &
    \draw[skip] (0,0) -- (0.8,0); \node[right=0.9cm]
            {\footnotesize Skip (concat)}; \\
    };

    \end{tikzpicture}%
    }
    \end{center}
    }
    }

    \block{3. Model Architectures}{
    \begin{minipage}[t]{0.45\linewidth}
        \centering
        \textbf{Motion Flow U-Net}
        
        \vspace{0.5em}
        \begin{tikzpicture}[node distance=1.1cm, scale=0.85, transform shape]
            \tikzstyle{layer} = [rectangle, draw, fill=blue!20, text width=2.5cm,
                                 text centered, minimum height=0.7cm, font=\small]
            \tikzstyle{flow} = [->, >=stealth, thick]

            \node (in)   [layer, fill=green!20]          {Input Image};
            \node (enc1) [layer, below of=in]            {Enc 64};
            \node (enc2) [layer, below of=enc1]          {Enc 128};
            \node (enc3) [layer, below of=enc2]          {Enc 256};
            \node (btl)  [layer, below of=enc3, fill=orange!20] {Bottleneck 1024};
            \node (dec3) [layer, right=2.5cm of enc3]    {Dec 512};
            \node (dec2) [layer, right=2.5cm of enc2]    {Dec 256};
            \node (dec1) [layer, right=2.5cm of enc1]    {Dec 128};
            \node (out)  [layer, right=2.5cm of in, fill=red!20] {Flow $(dx,dy)$};

            \draw [flow] (in) -- (enc1);
            \draw [flow] (enc1) -- (enc2);
            \draw [flow] (enc2) -- (enc3);
            \draw [flow] (enc3) -- (btl);
            \draw [flow] (btl) -| (dec3);
            \draw [flow] (dec3) -- (dec2);
            \draw [flow] (dec2) -- (dec1);
            \draw [flow] (dec1) -- (out);
            \draw [dashed, ->, gray] (enc1) -- (dec1);
            \draw [dashed, ->, gray] (enc2) -- (dec2);
            \draw [dashed, ->, gray] (enc3) -- (dec3);
        \end{tikzpicture}
    \end{minipage}
    \hfill
    \begin{minipage}[t]{0.45\linewidth}
        \centering
        \textbf{SVD (fine-tuned)}
        
        \vspace{0.5em}
        \begin{tikzpicture}[node distance=1.1cm, scale=0.85, transform shape]
            \tikzstyle{svd} = [rectangle, draw, fill=purple!20, text width=2.8cm,
                               text centered, minimum height=0.7cm, font=\small]
            \tikzstyle{sf} = [->, >=stealth, thick, purple!70!black]

            \node (img)  [svd, fill=green!20]             {Input Image};
            \node (clip) [svd, fill=blue!15, below of=img] {CLIP Encoder};
            \node (vae)  [svd, fill=blue!15, right=1.2cm of clip] {VAE Encoder};
            \node (cat)  [svd, fill=orange!15, below of=clip] {Concat latents\\(8 channels)};
            \node (unet) [svd, fill=orange!25, below of=cat, text width=2.8cm]
                         {Temporal U-Net\\{\scriptsize frozen spatial attn}\\{\scriptsize + fine-tuned temporal}};
            \node (dec)  [svd, fill=blue!15, below of=unet] {VAE Decoder};
            \node (vid)  [svd, fill=red!20,  below of=dec]  {$T$ video frames};

            \draw [sf] (img)  -- (clip);
            \draw [sf] (img)  -- (vae);
            \draw [sf] (clip) -- node[left, font=\scriptsize]{cross-attn} (cat);
            \draw [sf] (vae)  |- (cat);
            \draw [sf] (cat)  -- (unet);
            \draw [sf] (unet) -- (dec);
            \draw [sf] (dec)  -- (vid);
        \end{tikzpicture}
    \end{minipage}

    
}

    \column{0.5}
    \colorlet{blocktitlefgcolor}{white}
    \colorlet{blocktitlebgcolor}{purple!80!black}
    \colorlet{blockbodybgcolor}{purple!10}
    \colorlet{blockbodyfgcolor}{black}

    \block{4. Stable Video Diffusion (SVD) Approach}{
    \textbf{Pipeline overview:}
    \begin{itemize}
        \item YouTube drone videos → frame extraction with \texttt{frame\_gap=12} (captures $\sim$7s of real motion per clip)
        \item Motion filtering: $3.0 < \text{score} < 40.0$ (rejects static scenes and hard cuts)
        \item Fine-tuning via \textbf{SVD\_Xtend}: full UNet training, $lr=10^{-5}$, 5000 steps
    \end{itemize}

    \vspace{0.5em}
    \textbf{Key design choices:}
    \begin{itemize}
        \item \texttt{noise\_aug\_strength=0.02}: adds noise to conditioning frame, closes train/inference gap
        \item CFG dropout: enables guidance scale control at inference
        \item Post-processing: raw 7fps output slowed to 2fps $\rightarrow$ interpolated to 24fps via \texttt{ffmpeg minterpolate}
    \end{itemize}

    \vspace{0.5em}
    \textbf{Loss convergence (SVD\_Xtend, 2374 clips):}
    \begin{center}
    \begin{tabular}{|c|c|}
        \hline
        \textbf{Steps} & \textbf{Step Loss} \\
        \hline
        500   & $\sim$0.60 \\
        1000  & $\sim$0.45 \\
        3000  & $\sim$0.16 \\
        5000  & $\sim$0.20 \\
        \hline
    \end{tabular}
    \end{center}
}

    \block{5. Comparison of Techniques}{
        \begin{center}
        \begin{tabular}{|l|c|c|}
            \hline
            \textbf{Criterion} & \textbf{Motion Flow} & \textbf{SVD + LoRA} \\
            \hline
            Fidelity & Maximum & High \\
            Speed & Instant & Several seconds \\
            Weight & Light & Heavy \\
            Resources & CPU/GPU & High-End GPU \\
            \hline
        \end{tabular}
        \end{center}
    }

    \block{6. Result Evaluation}{
    \textbf{Qualitative comparison} (INPUT $\rightarrow$ GENERATED $\rightarrow$ GROUND TRUTH):
    \begin{itemize}
        \item SVD\_Xtend produces coherent drone-style camera motion on landscape images
        \item Motion fidelity improves significantly vs.\ naive LoRA ($\Delta \text{loss} \approx -0.30$)
        \item Remaining limitations: interpolation artifacts on high-frequency textures (dense foliage), and motion amplitude constrained by \texttt{motion\_bucket\_id}
    \end{itemize}

    \vspace{0.5em}
    \textbf{Ablation — impact of dataset quality:}
    \begin{center}
    \begin{tabular}{|l|c|c|}
        \hline
        \textbf{Setup} & \textbf{Final Loss} & \textbf{Motion quality} \\
        \hline
        LoRA, static clips        & 0.46 & None \\
        LoRA, motion-filtered     & 0.51 & Minimal \\
        SVD\_Xtend, motion-filtered & 0.16 & Cinematic \\
        \hline
    \end{tabular}
    \end{center}
}

    \block{7. Conclusion}{
        The hybridization of the two delivers the best of both worlds: structural speed and generative textual richness.
    }
\end{columns}

\end{document}
